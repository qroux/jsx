// import
import React from 'react';
import ReactDOM from 'react-dom';

// components
import App from './components/App';

// Append the component to the root div to render it
ReactDOM.render(
  <App />,
  document.querySelector('#root')
);
